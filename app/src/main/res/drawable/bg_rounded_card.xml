<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 카드의 배경색 -->
    <solid android:color="#FFFFFF"/>

    <!-- 둥근 모서리 설정 -->
    <corners android:radius="16dp"/>

    <!-- 테두리 (stroke) 설정 -->
    <stroke
        android:width="1dp"
        android:color="@color/green2"/>

    <!-- 그림자 대신 elevation을 사용할 수 있음 -->
</shape>